#ifndef MENU_HPP
#define MENU_HPP

#include "Graph.hpp"
#include <string>
#include <vector>
#include <map>
#include <list>

using namespace std;

void menu();
pair<string, string> seleccionarCiudades(Grafo &g);

#endif
